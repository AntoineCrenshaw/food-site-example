"use client";

export default function AccountPage() {
  return (
    <main className="w-full min-h-screen flex items-center justify-center px-6">
      <div className="w-full max-w-md rounded-2xl border border-gray-200 bg-white p-8 shadow-sm">
        <p className="text-xs uppercase tracking-widest text-[#b45e2a] mb-4">
          Account
        </p>
        <h1 className="text-4xl font-bold text-gray-900">
          Authentication is being moved
        </h1>
        <p className="text-gray-600 text-sm mt-3 leading-6">
          This app is being reset for a MERN-based authentication flow. You can
          replace this screen with your own login/register UI once your backend
          is ready.
        </p>
      </div>
    </main>
  );
}
